steps:
        # - name: 'gcr.io/cloud-builders/bazel'
        #  args: ['build', '...']
        - name: 'gcr.io/cloud-builders/mvn'
          args: ['install']
        - name: 'gcr.io/cloud-builders/docker'
          args: ['build', '--tag=gcr.io/$PROJECT_ID/dashboard', '.']
images: ['gcr.io/$PROJECT_ID/dashboard']

          #artifacts:
          #objects:
          #      location: gs://travis-content
          #      paths: ['./**/test.xml']
